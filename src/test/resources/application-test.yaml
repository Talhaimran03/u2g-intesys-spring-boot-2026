spring:
  application:
    name: teamboard
  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    username:
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

jwt:
  secret: test
  expiration: 604800000

server:
  base-path: /api
