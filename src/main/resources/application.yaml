spring:
  application:
    name: teamboard
  profiles:
    active: local
  datasource:
    url: jdbc:postgresql://localhost:5432/teamboard
    username:
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
