version: '3.8'

services:
  postgres:
    image: postgres:15
    container_name: teamboard-db
    environment:
      POSTGRES_DB: teamboard
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
    ports:
      - "5432:5432"